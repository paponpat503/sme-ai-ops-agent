# Screenshot Checklist

- Dashboard overview: capture `/demo` with the question, run metadata, first risk cards, and no browser chrome containing private information.
- Customer risk cards: show priority, action, evidence IDs, and draft message for one enterprise account.
- Tool trace: use `AGENT_MODE=tool_agent`; include planner, transport, call count, reasons, validation status, and latency.
- Tool registry demo: call `search_policy_docs` for enterprise refunds and show source, section, score, and stable chunk ID.
- README architecture diagram: capture the complete Mermaid diagram at readable scale.
- Eval output: show `pytest`, agent eval, retrieval metrics, and real MCP smoke test passing.

Use consistent 16:9 crops, remove API keys and terminal environment values, and verify customer data is the repository's synthetic dataset.
